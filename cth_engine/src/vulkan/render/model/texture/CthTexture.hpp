#pragma once
#include "vulkan/memory/image/CthImage.hpp"

namespace cth {
    class Texture : Image{
    public:
        struct Config;
    private:
    };
} //TEMP left off here, complete the texture class by automating the write and transition functions. 
